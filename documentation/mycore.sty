%URL vernünftig darstellen
\usepackage{url}

%A4 Seite besser ausnutzen
\usepackage{a4wide}

%zum einbinden von Programmcode
%\usepackage{listings}
%\lstset{language=Java} %: Sprachwahl - C, C++, Java, VHDL, Ada, Fortran, HTML, Mathematica, Pascal, Perl, Python..
%\lstinputlisting{meine-datei.java} : die einzufügende Datei

%Grafikeinbindung
 \newif\ifpdf 
 \ifx\pdfoutput\undefined 
   \pdffalse 
  \else 
   \pdfoutput=1 
   \pdftrue 
 \fi 

\ifpdf 
\usepackage[pdftex]{graphicx} 
\usepackage[pdftex, 
colorlinks, % Schrift in Farbe, sonst mit Rahmen 
bookmarksnumbered, % Inhaltsverzeichnis mit Numerierung 
bookmarksopen, % offnet das Inhaltsverzeichnis 
pdfstartview=FitH, % startet mit Seitenbreite 
linkcolor=blue, % standard red
citecolor=blue, % standard green 
urlcolor=magenta, % standard cyan 
filecolor=blue % 
]{hyperref} 
\pdfinfo{ 
          /Title (MyCoRe UserGuide) 
          /Subject (Manual) 
          /Author (Jens Kupferschmidt Frank Lützenkirchen) 
          /Keywords (MyCoRe) } 
\else 

   \usepackage{graphicx} 
    \usepackage{hyperref} 

     %fuer mit gzip gepackte Grafiken
        \DeclareGraphicsRule{.eps.gz}{eps}{.bb}{ gunzip -c #1} 
        \DeclareGraphicsRule{.egz}{eps}{.bb}{ gunzip -c #1} 
         
          \DeclareGraphicsRule{.jpg}{eps}{.bb}{`convert #1 'eps:-'}
          %Beispiel für jpeg2ps
          %\DeclareGraphicsRule{.jpg}{eps}{.bb}{`jpeg2ps #1}

          \DeclareGraphicsRule{.bmp}{eps}{.bb}{`convert #1 'eps:-'} 
          \DeclareGraphicsRule{.tif}{eps}{.bb}{`convert #1 'eps:-'} 
          \DeclareGraphicsRule{.gif}{eps}{.bb}{`convert #1 'eps:-'} 
          \DeclareGraphicsRule{.png}{eps}{.bb}{`convert #1 'eps:-'}


          %Bei den gepipten Anweisungen ist das  -  wichtig! 
          \DeclareGraphicsRule{.bmp.gz}{eps}{.bb}{ gunzip -c #1 | convert - eps:-} 
          \DeclareGraphicsRule{.bmz}{eps}{.bb}{ gunzip -c #1 | convert - eps:-}

\fi

%Fließumgebungen
\usepackage{float}
\usepackage{wrapfig}
\usepackage{picinpar}

%Kopf und Fußzeile
\usepackage{fancyhdr}
\pagestyle{fancyplain}

%\addtolength{\headwidth}{\marginparsep}
%\addtolength{\headwidth}{\marginparwidth}
\renewcommand{\chaptermark}[1]%
      {\markboth{#1}{}}
\renewcommand{\sectionmark}[1]%
      {\markright{\thesection\ #1}}

\lhead[\fancyplain{}{\nouppercase\thepage}]%
      {\fancyplain{}{\nouppercase\rightmark}}
\rhead[\fancyplain{}{\nouppercase\leftmark}]%
      {\fancyplain{}{\nouppercase\thepage}}
\cfoot{}

%alles was Tabellen brauchen
\usepackage{supertabular}
\usepackage{tabularx}
\usepackage{longtable}

  %schicke Tabellen
 \usepackage{booktabs}
 \usepackage{array}

 %  für gnumeric     	
        \usepackage{calc}
        \usepackage{multirow}
        \usepackage{hhline}
        \usepackage{ifthen}


%Einbinden von Programmcode
\usepackage{fancyvrb} 

%Querverweise
\usepackage{varioref}

%bunt
\usepackage{color,colortbl}

%drehen
\usepackage{rotating}
\usepackage{lscape}

%Beschriftungen
\usepackage[nooneline, float, longtable]{caption2}
\setlength{\belowcaptionskip}{2em}

%Absatz
\parindent0em 
\setlength{\parskip}{2ex}


\usepackage{makeidx}

\usepackage{nomencl}